{% extends 'WodaUIBundle::layout.html.twig' %}

{% block content %}

    <div>
        <form action="{{ path('WodaUserBundle.Profile.register') }}" method="post">
            {{ form_widget(form) }}
            <input type="submit" />
        </form>
    </div>

{% endblock %}
